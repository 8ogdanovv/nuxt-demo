{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "predeploy": "nuxt generate",
    "deploy": "npm run clear && npm run generate && npm run deploy-gh-pages",
    "clear": "rm -rf .output/public",
    "deploy-gh-pages": "git add . && git commit -m 'Deploy to gh-pages' && git push origin HEAD:gh-pages"
  },
  "devDependencies": {
    "@nuxt/postcss8": "^1.1.3",
    "@nuxtjs/color-mode": "^3.2.0",
    "@nuxtjs/tailwindcss": "^6.4.1",
    "autoprefixer": "^10.4.13",
    "gh-pages": "^5.0.0",
    "nuxt": "^3.2.2",
    "postcss": "^8.4.21",
    "tailwindcss": "^3.2.7",
    "tailwindcss-dark-mode": "^1.1.7"
  },
  "dependencies": {
    "@headlessui/vue": "^1.7.10",
    "@heroicons/vue": "^2.0.16"
  }
}
